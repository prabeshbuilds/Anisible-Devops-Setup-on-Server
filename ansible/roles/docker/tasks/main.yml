---
- name: Install Docker dependencies
  apt:
    name:
      - docker.io
      - python3-pip
    state: present
    update_cache: yes

- name: Start Docker
  service:
    name: docker
    state: started
    enabled: yes